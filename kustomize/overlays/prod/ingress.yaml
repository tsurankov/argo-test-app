---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appuio
spec:
  rules:
    - host: appuio-prod.local
      http:
        paths:
          - backend:
              service:
                name: appuio
                port:
                  number: 8080
            path: /
            pathType: Prefix
